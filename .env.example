# Admin credentials (change these in production!)
ADMIN_EMAIL=admin@ondetem.com
ADMIN_PASSWORD=change_me_in_production

# Server port
PORT=3000

# Allowed CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
